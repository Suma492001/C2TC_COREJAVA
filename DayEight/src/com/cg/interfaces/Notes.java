package com.cg.interfaces;

public class Notes {
	//Interface
	//1.Looks like class but it is not a class
	//2.keyword-interfaces .Only Class signature
	//3.Can have only abstract method and variables
	//4.variable-public static final
	//5. Method -public abstract is the default accesibility
	//6.class  - takes properties and behaviour of interface ==Implementable class
	//keyword- Implements
	
	//extends -->class  -class/interface-interface
	
	//implements--> class -interface 
	
	// interface -class --> it is not possible/compile time error  bcz

}
